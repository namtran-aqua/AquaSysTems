@using AntDesign
@using AquaSolution.Shared.ApprovalFlows
@using AquaSolution.Shared.CommonDto;
@using AquaSolution.Shared.UserManagements
@inject IMessageService Message
@{
    RenderFragment footer = @<Template>

        <Button OnClick="@SaveAsync" @key="@( "submit" )"
                Type="ButtonType.Primary">
            Save
        </Button>

        <Button Style="color:#808080" OnClick="@Close" @key="@("back")">Close</Button>

    </Template>;
}
<Modal @bind-Visible="IsModalVisible" Style="z-index: 9999;width:1000px;margin-top:-75px" Footer="@footer">
    <Form @ref="@formRef"
          Model="@CreatedUserDto"
          Layout="FormLayout.Vertical"
          OnFinish="SaveAsync"
          ValidateMode="FormValidateMode.Complex"
          ValidateOnChange="true">
        <Row Gutter="16">
            <Col Span="5">
            <FormItem Required="true" Label="FirstName">
                <TextArea ShowCount MinRows="1" MaxRows="1" @bind-Value="@CreatedUserDto.FirstName" AllowClear="true" />
            </FormItem>
            </Col>
            <Col Span="5">
            <FormItem Required="true" Label="LastName">
                <TextArea ShowCount MinRows="1" MaxRows="1" @bind-Value="@CreatedUserDto.LastName" AllowClear="true" />
            </FormItem>
            </Col>
            <Col Span="4">
            <FormItem Required="true" Label="WorkDayId">
                <TextArea ShowCount MinRows="1" MaxRows="1" @bind-Value="@CreatedUserDto.WorkDayId" AllowClear="true" />
            </FormItem>
            </Col>
            <Col Span="10">
            <FormItem Required="true" Label="Email">
                <TextArea ShowCount MinRows="1" MaxRows="1" @bind-Value="@CreatedUserDto.Email" AllowClear="true" />
            </FormItem>
            </Col>
        </Row>
        <Row Gutter="16">
            <Col Span="6">
            <FormItem  Label="PhoneNumber">
                <TextArea ShowCount MinRows="1" MaxRows="1" @bind-Value="@CreatedUserDto.PhoneNumber" AllowClear="true" />
            </FormItem>
            </Col>
            <Col Span="7">
            <FormItem  Label="Position">
                <Select TItem="BaseDto" TItemValue="Guid?"
                        DataSource="@ListPosition"
                        @bind-Value="@CreatedUserDto.PositionId"
                        AllowClear="true"
                        ItemValue="c => c.Id"
                        ItemLabel="c => c.Name">
                </Select>
            </FormItem>
            </Col>
            <Col Span="7">
            <FormItem  Label="Factory">
                <Select TItem="BaseDto" TItemValue="Guid?"
                        DataSource="@ListFactory"
                        @bind-Value="@CreatedUserDto.FactoryId"
                        AllowClear="true"
                        ItemValue="c => c.Id"
                        ItemLabel="c => c.Name">
                </Select>
            </FormItem>
            </Col>
            <Col Span="4">
            <FormItem  Label="FlowApproval">
                <Select TItem="ApprovalFlowDto" TItemValue="int?"
                        DataSource="@ListApprovalFlow"
                        @bind-Value="@CreatedUserDto.FlowApproval"
                        AllowClear="true"
                        ItemValue="c => c.FlowApproval"
                        ItemLabel="c => c.FlowApproval.ToString()">
                </Select>
            </FormItem>
            </Col>

        </Row>
        <Row Gutter="16">
            <Col Span="12">
            <FormItem  Label="Department">
                <Select TItem="BaseDto" TItemValue="Guid?"
                        DataSource="@ListDepartment"
                        @bind-Value="@CreatedUserDto.DepartmentId"
                        AllowClear="true"
                        ItemValue="c => c.Id"
                        ItemLabel="c => c.Name">
                </Select>
            </FormItem>
            </Col>
            <Col Span="12">
            <FormItem Label="Manager">
                <Select TItem="UserContributerDto" TItemValue="Guid?"
                        DataSource="@AllManagers"
                        @bind-Value="@CreatedUserDto.ManagerId"
                        AllowClear="true"
                        EnableSearch
                        AutoClearSearchValue="true"
                        ItemValue="c => c.Id"
                        ItemLabel="c => c.Name">
                </Select>
            </FormItem>
            </Col>
            @if (IsEdit)
            {
                <AntDesign.Col Span="2">
                    <FormItem Label="IsActive">
                        <Switch @bind-Value="@CreatedUserDto.IsActive" />
                    </FormItem>
                </AntDesign.Col>
            }

        </Row>
    </Form>
</Modal>

